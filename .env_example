# OpenAI / AutoGen config
# This is a JSON array; Autogen v2 uses it via OAI_CONFIG_LIST
OAI_CONFIG_LIST=[{"model": "gpt-4.1-mini","api_key": "stuff","base_url": "https://api.openai.com/v1"}]

OPENAI_API_KEY="stuff"
# GitHub token with repo access
GITHUB_TOKEN="stuff"

# Optional custom CA bundle for SSL (e.g., certs.pem)
# SSL_CERT_FILE=/app/certs/certs.pem
# or:
# REQUESTS_CA_BUNDLE=/app/certs/certs.pem
